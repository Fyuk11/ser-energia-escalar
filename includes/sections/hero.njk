<section class="hero" id="hero">
  <!-- Videos -->
  {% if section_data.videos %}
    <video class="hero-video hero-desktop" autoplay muted loop playsinline>
      <source src="{{ section_data.videos.desktopWebm }}" type="video/webm">
      <source src="{{ section_data.videos.desktopMp4 }}" type="video/mp4">
    </video>
    <video class="hero-video hero-mobile" autoplay muted loop playsinline>
      <source src="{{ section_data.videos.mobileWebm }}" type="video/webm">
      <source src="{{ section_data.videos.mobileMp4 }}" type="video/mp4">
    </video>
  {% endif %}

  <!-- Overlay con gradiente animado -->
  <div class="hero-overlay"></div>
  <div class="hero-fallback"></div>

  <!-- Partículas flotantes -->
  <canvas id="heroParticles" class="hero-particles"></canvas>

  <!-- Contenido central -->
  <div class="hero-content-wrapper">
    <div class="hero-content">
      <h1 class="fade-left animate-on-scroll">
        {{ section_data.title or landing.title }}
      </h1>
      <p class="fade-left delay-1 animate-on-scroll">
        {{ section_data.subtitle or landing.description }}
      </p>

      {% if section_data.ctas %}
        <div class="hero-ctas">
  {% for cta in section_data.ctas %}
    <a href="{{ cta.url }}"
       class="btn-primary fade-left delay-{{ loop.index }} scroll-link animate-on-scroll"
       {% if "http" in cta.url %} target="_blank" rel="noopener noreferrer"{% endif %}>
      {{ cta.text }}
    </a>
  {% endfor %}
</div>

<!-- dentro de <section id="hero" class="hero"> -->
<!-- Mandala decorativo (aria-hidden porque es decorativo) -->
<div class="hero-mandala" aria-hidden="true">
  <!-- ejemplo simple: reemplazá por tu SVG más elaborado -->
  <svg viewBox="0 0 500 500" xmlns="http://www.w3.org/2000/svg" class="mandala-svg">
    <defs>
      <linearGradient id="goldGrad" x1="0" x2="1">
        <stop offset="0" stop-color="#f7f3e6"/>
        <stop offset="1" stop-color="#d4af37"/>
      </linearGradient>
    </defs>

    <g fill="none" stroke="url(#goldGrad)" stroke-width="2" opacity="0.14">
      <!-- círculos concéntricos -->
      <circle cx="250" cy="250" r="120"/>
      <circle cx="250" cy="250" r="170"/>
      <circle cx="250" cy="250" r="220"/>
      <!-- pétalos simples -->
      <g transform="translate(250,250)">
        <!-- repetir con transform rotado para simular mandala -->
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(0)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(45)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(90)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(135)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(180)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(225)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(270)"/>
        <path d="M0,-120 C20,-80 20,-40 0,-0" transform="rotate(315)"/>
      </g>
    </g>
  </svg>
</div>

      {% endif %}
    </div>
  </div>
</section>
