<section class="mission-section" id="mission">
  <div class="container text-center animate-on-scroll fade-up">
    {% if section_data.title %}
      <h2 class="section-title">{{ section_data.title }}</h2>
    {% endif %}

    {% if section_data.text %}
      <p class="section-subtitle">{{ section_data.text }}</p>
    {% endif %}
  </div>
</section>
