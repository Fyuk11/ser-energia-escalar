<nav class="nav nav--white">
  <div class="nav__inner container">
    <!-- Brand -->
<a class="nav__brand" href="/">
  <img src="{{ site.logo or '/assets/images/logo.png' }}" alt="Ser Energía Escalar" class="nav__logo">
  <span>{{ site.name or "Ser Energía Escalar" }}</span>
</a>


    <!-- Burger toggle (mobile) -->
    <button class="nav__toggle" aria-label="Abrir menú">
      <span></span>
    </button>

    <!-- Menú -->
    <ul class="nav__menu" id="navMenu">
      {% if landing.nav %}
        {% for item in landing.nav %}
          <li>
            <a 
              href="{{ item.url }}" 
              {% if item.external %}target="_blank" rel="noopener"{% endif %}
            >
              {{ item.text }}
            </a>
          </li>
        {% endfor %}
      {% else %}
        <li><a href="#hero">Inicio</a></li>
        <li><a href="#benefits">Beneficios</a></li>
        <li><a href="#process">Cómo funciona</a></li>
        <li><a href="#demo">Demo</a></li>
        <li><a href="#contact">Contacto</a></li>
      {% endif %}
    </ul>
  </div>
</nav>
